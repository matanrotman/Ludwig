# Project Status

*The current snapshot only — replace sections when they change, don't append to them. Detailed history lives in each feature's spec, under its own "Session Log."*

**Last updated:** 2026-07-20 (session 4) (**Two RTL selection items + a follow-up round of three small fixes, all DONE and live-verified.** RTL selection-jump bug fixed; Option+Ctrl+Shift+arrow selection shortcuts built and then made **repeatable line-by-line** on user feedback; ribbon collapse default Cmd+Option+R. Follow-up round: **arrow-up now walks a wrapped first block's own lines before the title** (was jumping straight to title); **dark/light now splits — the toggle recolors the page+desk, and the app frame gets its own "App frame appearance" setting** (default "Same as pages" = unchanged). Fork commits `e7a3942e`+`806f9b3a` pushed + pin re-synced; dock app rebuilt + content-verified. ⚠️ Computer-use click-offset hazard recorded below, and **three junk test pages await user deletion**: "zz scratch - selection test" + two empty "Untitled".)

*Prior session (2026-07-20 session 3):* (**Phase 1 verified live, Phase 2 built and verified, RTL margins FIXED and measured, three user requests done.** Phase 1's unverified items were driven in the real app: tooltips show name+shortcut, button actions work, pending format works, floating toolbar suppressed, restart persistence holds. **The collapse shortcut does not fire — not a code bug**: `fnState` is unset on this Mac so F1 sends brightness-down, and Ctrl+F1 is a macOS system shortcut. Needs a different default. **The ribbon now mirrors the SIDEBAR's dock side** (user reversed the always-LTR decision) — note it must follow `SidebarDockSide`, *not* `LayoutDirection`. **Title/icon now follow the page's direction**, fixing the reported "title doesn't move when I set LTR". **New: the page renders as a sheet on a desk.** Margins measured ≈104/104 (was 97/116). All committed.)

*Prior session (2026-07-18):* (**Project stance set: personal build now, designed to open to others later — docs only, no code.** The multi-user design rule now lives in `CLAUDE.md` ("Designing for other users, not just me"); per-feature readiness notes were folded into each spec; `specs/distribution.md` created as the placeholder for "others download my macOS build instead of compiling." **Identity decision (leaning, to confirm): no rebrand** — keep the AppFlowy name + an honest qualifier, change only the bundle id + data-folder location so it coexists with official AppFlowy. **Correction worth knowing: the backup feature is already usable by other people** — code-verified, auto-detect isn't pinned to my account and a manual folder picker already exists; it needs zero changes for a macOS release. **Next session: the ribbon scoping interview** — unchanged, still the active feature. Google Drive backup Stage 5 drills still await the user.)

*Prior session (2026-07-17):* (**Plugin-system question RESOLVED — investigation only, no code.** Deep-research harness + codebase exploration concluded, user signed off: no plugin system gets built; the ribbon menu and future features are built as **sidecar modules** (new files behind a feature flag, ~15-line core-touch budget, the backup pattern). Runtime plugins rejected outright — AppFlowy's own team tried dart_eval and publicly abandoned it. Full verdict, evidence, and the prepped ribbon scoping interview: `specs/plugin-system.md`. **Next session: the ribbon scoping interview** — ribbon first, right-click menu second, `specs/ribbon-menu.md` after the interview. Google Drive backup Stage 5 drills still await the user (unchanged from 2026-07-16).)

## ⚠️ CLOUD SYNC IS DEAD — proven end-to-end 2026-07-16. The user's writing exists ONLY on this Mac.
**Do not trust AppFlowy Cloud as a backup, and don't spend time "fixing" sync — it is not fixable locally.**
- `beta.appflowy.cloud` blocks the old realtime protocol (`/ws/v1`) at its edge: **426 `{"error": "Please upgrade to latest client"}`** — for every version string (tested 0.7.9→99.0.0), any User-Agent, valid token or none, from Israel and from the US. Proven with a Rust probe using the app's **exact** stack (tokio-tungstenite 0.20.1 + native-tls, same headers): `scratchpad wsprobe` result identical to curl/python, so it's not TLS fingerprinting.
- **The official binaries are NOT built from the public source.** The user's very first run (2026-07-11 19:30, the official appflowy.com download, before any local build) logged `client_api::v2::actor` — a module that does **not exist** in the public `client-api` pin (`592f644`, May 2025) that the 0.12.5 release tag builds (verified: tag's Cargo.toml pins it exactly, no `[patch]` override). Official users run a private v2-protocol client via auto-update; that's why nobody else complains. Corroboration: server self-reports **0.17.10** while the public AppFlowy-Cloud repo tops out at **0.9.64**; public app repo has **zero** v2 glue (`grep -rn "client_api::v2\|WorkspaceController" rust-lib` = empty); GitHub issue 2026-07-02 "Stop lying and spamming that this is OSS".
- **Consequence for every source-built AppFlowy (ours included): pages are created on the cloud as EMPTY SHELLS over HTTP; typed content rides the blocked websocket and never arrives.** Final proof: doc `c4606ec3-…` (page `b7257738-…`) = **146 blocks locally** (children_map in `log.sync.2026-07-15-20`) vs **741-byte skeleton with 1 empty paragraph** fetched from the cloud (`GET /api/workspace/v1/{w}/collab/{id}?collab_type=0`, Bearer token, HTTP 200).
- Why the app never surfaced it: `client-api`'s retry loop swallows the error (`retry.rs:119` discards it unlogged; only `AuthError` stops retrying, and the 426 maps to `WSError::Http`) → silent 15s-interval reconnect forever (~4.5k attempts/day). `🟢websocket connect success` appears **zero times in every log on this machine, ever**.
- **Decision (user, 2026-07-16): replace the safety net with automatic backups to Google Drive** — `specs/google-drive-backup.md`. **Stages 1–2 are BUILT, live-verified, and committed (`a37665df3`) — the app is right now snapshotting into the user's real Google Drive** (`My Drive/AppFlowy Backups/snapshots/`, every 30 min when changed + on quit + a 60s post-launch catch-up; 9 snapshots landed on 07-16 alone, on schedule). No Google auth code — the already-running Drive desktop app does the uploading. **Stage 3 (settings page) is BUILT and committed (`9abdb7c4b`). Stage 4 (guarded restore + `RESTORE.md`) is BUILT, gate-passed, and COMMITTED (`08066e099`)** — Restore… buttons in Settings → Backup now run the real flow. Stage 5 (drills) is partially done; the rest needs the user.
- **The restore machinery is now proven two ways** (2026-07-16 r4): a full scratch-folder round-trip on a real filesystem (real zips, real renames, rollback verified byte-for-byte), and `RESTORE.md`'s fully-manual route rehearsed against the newest REAL snapshot (unzip -t clean, manifest correct, 120/120 files match the live folder). **What's still unproven: the in-app GUI drill — Stage 5(f) — where the user watches both safeguards fire.** Keep `~/Desktop/AppFlowy_data_backup_backup_20260715_0925` until (f) passes; after that the Drive net stands on its own.
- Diagnostic artifacts kept in the session scratchpad: `wsprobe/` (Rust replica probe), `ws-trace.log` (RUST_LOG=trace run), `cloud_doc.json` (the empty cloud copy). The probe technique is reusable if AppFlowy ever changes the edge behavior.
- **Possible community service (not scheduled):** a well-evidenced upstream issue "source-built AppFlowy cannot sync with official cloud." All evidence is reproducible; user hasn't decided whether to file it.

## Project stance — personal build now, designed to open to others later (set 2026-07-17)
This fork is being positioned so it *could* one day be used by other people — especially those who want RTL support, local backup, and the roadmap features. **Nothing needs to work for everyone today.** The rule (full version in `CLAUDE.md` → "Designing for other users, not just me"): a feature may ship local-only-for-me first, but must be *designed* so a later "make it multi-user" step is a small, bounded edit — not a rewrite. Personal-only assumptions get isolated and named, not baked in.
- **Distribution goal:** others should eventually **download my prebuilt build (macOS first), not compile their own.** This is captured as its own feature in `specs/distribution.md` — **not started, needs its own scoping session.** It carries real cross-cutting questions (app identity/bundle-id collision with official AppFlowy, naming + AGPL obligations, code-signing/notarization, what server/data-dir a downloaded build defaults to, updates). Keep those in mind when a feature touches app identity, data location, or the default server.

### Multi-user readiness at a glance (detail lives in each feature's spec)
- **Google Drive backup** — *more multi-user-ready than it looks (code-verified 2026-07-18).* Other people **can already use it**: auto-detect scans for *any* `GoogleDrive-*` mount (not my account), a **manual folder picker already exists** (`backup_bloc.dart:80` `pickDestination()`), so non-Drive users point it at Dropbox/iCloud/any folder, and it **fails soft** (no mount → null → "no destination" idle, no crash). The only genuinely macOS-specific part is the *auto-detect convenience* (`~/Library/CloudStorage` layout + `HOME` env, unset on Windows) and the Drive-flavored labels. **Needs zero changes for a macOS distribution.** See `specs/google-drive-backup.md` → "Multi-user readiness."
- **RTL/LTR support** — *the headline draw for other users, mostly universal already.* Watch: behaviors that assume my personal `kDocumentAppearanceDefaultTextDirection = rtl` default must work for LTR/auto users too. Split across app + editor fork (fine for a downloaded binary; source-builders need the fork pin). See `specs/rtl-support.md` → "Multi-user readiness."
- **Floating toolbar / ribbon (upcoming)** — universal, no personal assumptions; ribbon interview should bake in the multi-user design rule. See `specs/plugin-system.md`.
- **Meeting transcription (stub)** — *inherently personal* (my self-hosted server); a general version needs the server endpoint to be user-config, not hardcoded. See `specs/meeting-transcription.md`.
- **Tables (RTL paste, spec only)** — universal; minor note in `specs/tables.md`.

## Active feature
**Ribbon menu — PHASES 1 & 2 BUILT AND VERIFIED (2026-07-20 session 3).** `specs/ribbon-menu.md` is the active document.
- **Behind `FeatureFlag.ribbonMenu`, OFF by default**; Settings → Feature Flags, then restart. Currently **ON** in the user's prefs.
- **Phase 1 is now fully verified on the real macOS target** — the 2026-07-19 gap is closed. Tooltips show name+shortcut; the greyed no-cursor state explains itself ("Place your cursor in the document first"); button actions apply with correct highlight states; Word-style **pending formatting** works (click Underline with only a caret, then type); the floating toolbar stays suppressed; collapse + active tab survive a restart.
- **Phase 2 (per-page RTL/LTR) is built and verified.** Direction lives in `View.extra` with `inherit` modelled as *absence* of the key, so untouched pages behave exactly as before. Persists across reopening.
- **The ribbon mirrors the SIDEBAR's dock side** (reverses the 2026-07-19 always-LTR call, user's request 2026-07-20). ⚠️ It follows `SidebarDockSide`, **not** `LayoutDirection` — they are deliberately independent, and choosing the wrong one silently does nothing (this user runs an LTR interface with the sidebar docked right). A per-page direction switch still must never rearrange the toolbar.
- **The page now renders as a sheet on a desk**: own surface, shadow, **square corners**, **three edges** (top/left/right, running off the bottom), 32pt margin, and the top gap **closes as you scroll down** so the page slides against a fixed desk. Sidecar `page_surface.dart`; the desk colour is derived in HSL because the theme tokens collapse (light theme's `primary` and `layer02` are the same white).
- **The page title and icon follow the PAGE's direction**, not the app layout's — the reported "title doesn't move when I set LTR".
- **⚠️ The collapse shortcut does not fire, and it is NOT a code bug.** `com.apple.keyboard.fnState` is unset on this Mac, so F1 sends brightness-down and the app never sees the key; Ctrl+F1 is also a macOS system shortcut. The binding parses and is registered correctly. **Deferred with the rest of the shortcuts work** (user, 2026-07-20).
- **Future button designs captured** (build later): font picker = dropdown with filter box, each font name in its own font; font size = type-in box flanked by carets; editable **paragraph-style hierarchy** à la Google Docs (likely its own spec).

### ⚠️ Blank window: diagnosed, NOT a bug in this app
Most of the 2026-07-19 session was lost to AppFlowy showing a pure black window. **It was not the ribbon, not the data, not the build.** The Flutter surface only presents a frame when the window receives an input event — an app launched from a shell (or `open`) and screenshotted without clicking paints black indefinitely. Evidence: `sample` showed *every* Flutter thread parked in `mach_msg2_trap` with nothing queued; the Rust core was healthy throughout; `runApp` definitely ran; and in debug `ErrorWidget.builder` paints a **red** bar on build errors — we only ever saw black. A stash-and-rebuild of pristine code reproduced it, which exonerated the ribbon.
**How to avoid burning time on this again:** after launching for visual checks, **click inside the window** before trusting a screenshot — but click **neutral chrome** (sidebar, ribbon, title bar), never the page body (see the incident-history note below). Launching from the Dock delivers a proper activation event and very likely avoids it entirely.

**Google Drive backup** (`specs/google-drive-backup.md`) — **ALL 5 STAGES ESSENTIALLY DONE. The safety net is proven end-to-end (2026-07-19).**
- **Stage 5 drill (f) — the headline — PASSED.** A real in-app restore ran against the user's real data folder, and both safeguards were verified on disk afterwards: `AppFlowy-prerestore-v0.11.4-20260719-015952.zip` in the snapshots folder, and a `data_dev_beta.appflowy.cloud.pre-restore-20260719-015952` folder beside the live data. **The 2026-07-15 Desktop backup folder is now officially retirable.**
- **Retention rebuilt as a tiered ladder** (user's choice): everything < 2 days (cap 50) → daily to 30 days → weekly to 6 months → monthly to 1 year → deleted. The old policy (newest 50 + one per day *forever*) never expired anything: ~550MB/year, unbounded. Steady state now ~60–80MB. Pre-restore snapshots stay exempt, capped at 3. Pruning ran live and behaved exactly as predicted (9 same-day duplicates collapsed, newest-per-day survived).
- **Snapshots now exclude logs + derived search indexes** — 78 files / **63% of raw bytes** on the real folder. Matching is on path segments/basenames, never substrings, so user files can't be caught (explicitly tested). `collab_db_history` is **deliberately kept** as a second independent copy. The manifest records `exclusionRules`/`excludedFileCount` so absent files read as *by design*, not lost; `formatVersion` stays 1 and old snapshots still restore.
- **Measured, not guessed:** compression was already solved (zip: 5.2MB raw → 1.4MB). Logs are 49% of raw bytes but only ~13% of the zip. **Deduplication was considered and deliberately rejected** — it would break `RESTORE.md`'s plain-`unzip` guarantee for a ~10MB win.
- **Known follow-up, not urgent:** `collab_db_history` grows unboundedly *inside* every snapshot — AppFlowy writes one internal archive per day and never deletes them (already 1.3MB of a 1.9MB zip; 365 of them in a year). Capping them at the newest ~3 inside snapshots would keep the redundancy and bound the growth. Small change.
- Remaining Stage 5 drills, all optional now that (f) passed: (a-web) confirm in drive.google.com; (c) evict-and-restore an offloaded snapshot; (d)/(e) quit + `kill -9` paths; (g) seeded-prune drill (needs the user's go-ahead — touches the real Drive).

*(Historical: Stages 1–4 detail.)* **Stages 1–4 done and committed.** Session 1 (Stages 1–2: engine + scheduling): committed, live. Session 2 (Stage 3: settings page): committed (`9abdb7c4b`). Session 3 (Stage 4: guarded restore + `RESTORE.md`): built, tested (54/54), gate-passed, shipped to the dock app, **committed (`08066e099`)**. **What remains is Stage 5's user-present drills** — the full list with per-item state is in the spec's Session Log (2026-07-16 Session 3 entry); the headline one is **(f): the in-app restore drill on a scratch data folder, both safeguards witnessed**. Drills (b) and (h) already pass; the manual-restore route is proven against a real snapshot. This is likely a short Session 4: just the drills, no more code expected unless one surfaces a bug.

**Floating selection toolbar pointer-anchor experiment — DONE, this session, 4 live-tested rounds.** The user's closing verdict: *"very happy with this... some small minor quirks."* No further work is scheduled — if a future session picks this up, start by asking what the quirks are rather than guessing. Full detail: "Bug status" below.

RTL/LTR support (`specs/rtl-support.md`) is otherwise parked: Phase 1 (sidebar) done and signed off. Phase 2 (document-content direction) in good shape: auto-detect direction, bidi, block-insert menu direction, icon-margin gap, and the **empty-line cursor** are all done. Bidi arrow-key navigation is scoped (root cause pinned; visual-movement behavior decided; own-session-sized) but not built. Also parked: `specs/plugin-system.md` (stub — the user wants to entertain a real plugin system; investigation groundwork recorded, interview not started).

## The editor-fork merge is deferred on purpose — don't restart it from the "36 behind" number
The fork-sync check will keep reporting the editor fork as **36 behind / 10 ahead**. That is true and
still the wrong target to chase. `AppFlowy-IO/AppFlowy` pins `appflowy_editor` at `470c4e7`, which is
**byte-identical to our merge base** — against the upstream that actually matters we are **0 behind**,
and those 36 commits belong to a package the app doesn't follow. 35 of them sit at/after a Flutter
≥3.32 bump (upstream CI runs 3.38.5) while the app, AppFlowy-IO's CI and this machine are all on
3.27.4, so `pub get` fails before anything compiles. It is a Flutter migration of all of AppFlowy, not
an editor merge, and it would land us on an untested AppFlowy 0.11.4 + editor 6.1.0 while *increasing*
what we maintain. **Trigger to revisit: when AppFlowy-IO bumps its own editor pin past `470c4e7`.**
Full reasoning, the landmines, and the convergent-conflict finding: `specs/rtl-support.md` →
"Editor fork — upstream merge".

## How to verify anything here (read this first — these rules are why the loop broke)
**Several "still broken" bugs turned out to be already fixed — the user was testing a STALE dock app** (found 2026-07-15 r1). The floating toolbar was the clearest case: it worked in a fresh build and was broken only in the installed app, which predated the fix. That explained the multi-session loop: **the fixes were real but never reached the app being tested in.** Everything below exists to stop that recurring — treat it as standing procedure, not history.
1. **Verify against the REAL macOS render path, never headless `flutter test`.** Run `flutter test integration_test/desktop/document/<file>.dart -d macos`. Plain `flutter test` forces a fixed-width fake font (Ahem) that collapses RTL glyph geometry, so RTL caret/position bugs are invisible to it — this is why prior headless "fixes" went green while the app stayed broken. (It gets worse: even on the real target, `editorState.selectionRects()` mis-reports the caret for the shrink-wrapped empty RTL block — measure the actual rendered `Cursor` widget's global rect instead.)
2. **The user's dock app IS the DEBUG build — verified from the Dock plist, don't re-guess this.**
   `~/Library/Preferences/com.apple.dock` → the AppFlowy tile points at
   `…/frontend/appflowy_flutter/build/macos/Build/Products/Debug/AppFlowy.app`.
   So: their real pages live in `…/com.appflowy.appflowy.flutter/**data_dev_beta.appflowy.cloud**` (debug + AppFlowy Cloud), **not** `data_beta.appflowy.cloud` (that's a stale release-build cache — a *different* local cache of the same cloud workspace `612287731153768448`).
   **To ship a fix to the user's daily app: `flutter build macos --debug`** — it rebuilds *in place* at the exact path the dock icon already points to. No release build, no `/Applications` copy, no dock changes needed. (A release build was tried on 2026-07-15 r1 and was the wrong target: it opened `data_beta`, so recent pages looked "missing." Nothing was lost.)
   Data-dir map: debug → `data_dev*`; release → `data*`; integration tests → their own sandbox (see the hazard below).
   Note for debug bundles: the Dart code is `Contents/Frameworks/App.framework/Resources/flutter_assets/kernel_blob.bin`. **Do NOT judge a rebuild by its timestamp** (corrected 2026-07-15 r2 — the earlier advice here was wrong): Flutter copies the cached artifact and *preserves its mtime*, so a successful rebuild can legitimately show an old time. A verified-correct restore on 2026-07-15 r2 read `09:26` — hours stale, and fine. Check **contents**, not time:
   ```
   KB=build/macos/Build/Products/Debug/AppFlowy.app/Contents/Frameworks/App.framework/Resources/flutter_assets/kernel_blob.bin
   strings "$KB" | grep -c IntegrationTestWidgetsFlutterBinding   # want 0 (non-zero = it's a TEST build)
   strings "$KB" | grep -c runAppFlowy                            # want >0 (the real app)
   ```
   (Grepping the blob for a *test filename* is a false-positive detector — it matches a code comment in `appflowy_rich_text.dart` that references the test file.)
3. **⚠️ HAZARD — `flutter test integration_test/... -d macos` OVERWRITES the dock app with a TEST build.** (Found 2026-07-15 r2.) The run rebuilds *in place* at `build/macos/Build/Products/Debug/AppFlowy.app` — the exact path the Dock tile points at — using the test as the Dart entrypoint. Verified: afterwards the bundle carried **14** `IntegrationTestWidgetsFlutterBinding` refs. Clicking the dock icon then launches the test harness, not AppFlowy. This is silent, and is very likely a *second* cause of the "blank window / looks broken" reports previously blamed only on the pref below.
   **Always re-run `flutter build macos --debug` after any `-d macos` integration test**, then verify by contents as above.
4. **⚠️ HAZARD — integration tests pollute the user's REAL app preferences.** `initializeAppFlowy()` → `mockApplicationDataStorage()` writes
   `flutter.io.appflowy.appflowy_flutter.path_location` into `~/Library/Preferences/com.appflowy.appflowy.flutter.plist`, which is **shared by every build (same bundle id)**. The user's app then opens an empty integration-test sandbox under `~/Library/Caches/appflowy_integration_test/…` and shows a **blank window** — which looks exactly like a broken build. Their data is fine; the app is just looking in the wrong folder.
   **After ANY integration-test run, clear it before the user opens their app:**
   ```
   defaults delete com.appflowy.appflowy.flutter flutter.io.appflowy.appflowy_flutter.path_location
   ```
   (Also note `syncDefaultTextDirection(...)` in tests writes `kDocumentAppearanceDefaultTextDirection` to those same real prefs — it happened to match the user's existing `rtl`, but don't rely on that.)

## Bug status
- **Arrow-up jumped straight to the title from a wrapped first block — FIXED, live-verified (2026-07-20 session 4 r2).** Commit `7041a0bfa`. `arrowUpToTitle` gated only on "caret in the first block," so when that block wrapped, plain arrow-up from a lower visual line skipped the block's own upper lines. Now it also requires the caret's visual line to start at offset 0 (via the fork's `getLineBoundaryInPosition`, falling back to the old behavior if the render object is unmounted). Verified live: line 3 → line 2 → line 1 → title, one press each.
  - **⏸ Upstream PR PREPARED but NOT pushed (awaiting user go, 2026-07-20 r3).** Vanilla AppFlowy has the identical bug (confirmed against `upstream/main`). Local branch **`fix/arrow-up-to-title-first-visual-line`** (off `upstream/main`, commit `38be8900c`, one file +36) holds an **upstream-compatible rewrite**: our in-app fix uses the fork-only `getLineBoundaryInPosition`, which vanilla's editor lacks, so the PR version instead compares caret rects via `getCursorRectInPosition` (a real vanilla `SelectableMixin` API) to detect the first visual line. The user said "let's do it," but the follow-up confirm-to-push question went unanswered, so nothing was pushed. **To ship it:** `git push origin fix/arrow-up-to-title-first-visual-line` then `gh pr create` against `AppFlowy-IO/AppFlowy:main` (CLA check will run; user has signed AppFlowy's CLA before). PR title: "fix: arrow-up only jumps to the title from the first visual line".
- **Dark/light: layout vs. per-page — REWORKED to the right model, live-verified (2026-07-20 session 4 r2→r4).** Final commit `4762b470d` (supersedes the r2/r3 attempts `3ba5f93b8`, `0b818f19d`, which had it inverted). **The user's rule: Settings control the layout + default page look; the ribbon controls this page's look.**
  - **Settings → Appearance drives the whole app** (chrome + any page that inherits) — reverted the earlier chrome/global split, so setting it Light makes the *layout* light again (the earlier build made that impossible). This is just upstream's original `themeMode` behavior, restored.
  - **The ribbon "Appearance" button is now a PER-PAGE override**, stored in `View.extra` (`page_theme_mode.dart`), mirroring the per-page text direction exactly: `inherit` = absence of the key, so untouched pages follow the app theme unchanged. Pressing it flips the current page's effective brightness; flipping back onto the app theme clears the override to `inherit` so the page keeps following Settings. `PageThemeScope` applies the override to the document subtree (page + desk) only; no-op on inherit.
  - **Removed entirely:** `ChromeThemeMode`, the "App frame appearance" settings row, its KV key, translations, and the freezed field — the whole chrome-split concept was the wrong design.
  - **Verified live end-to-end:** Settings → Light lit the entire layout; the ribbon darkened only the current page while the sidebar/top-bar/ribbon stayed light; a second page stayed light (inherit) while the overridden page kept its dark across switch-away-and-back; Settings → Dark restored a uniform dark app. App left on the user's usual all-dark.
  - ⚠️ Tooling note kept: the Settings dialog scrolls fine by hand but **not via computer-use**; the top Appearance radios (Auto/Light/Dark) are reachable, deeper rows are not.
- **Per-page theme: text + link colors — FIXED, live-verified (2026-07-20 session 4 r5).** Commit `bbd1c386a`. Two problems once a page override differed from the layout:
  1. **Text/link kept the LAYOUT theme's colors** (dark text on a dark page, and vice versa) because `EditorStyleCustomizer` reads all colors from the context it's *constructed* in — which sat above `PageThemeScope`. Fixed by building the editor (style customizer + cover/title) inside a `Builder` **under** `PageThemeScope`, so `Theme.of(context)` resolves to the page theme and every theme-derived color (text, links, code, callout, selection…) follows the page. The mobile path (no PageThemeScope) uses the outer context unchanged.
  2. **Links were washed out on light pages** — the default cyan `#00BCF0` is only **2.2:1** on a white sheet (WCAG AA body text wants 4.5:1). New `util/color_contrast.dart` (`ensureContrast`: WCAG ratio + hue-preserving lightness nudge) darkens it to `~#007C9E` (4.8:1) on white and leaves it untouched on dark (7.3:1, already fine). Applied to the href color in `editor_style.dart` (both desktop + mobile).
  - The "test to measure clearness" the user asked for is `test/unit_test/util/color_contrast_test.dart` — 10 tests asserting the real WCAG ratios (black/white=21, default text passes both sheets, raw link fails on white → fixed, untouched on dark). Verified live: light layout + dark page override → readable light text + crisp cyan link; toggle that page light → dark text + darker legible blue link.
  - **Note:** the junk scratch page now has two extra test lines (a raw URL and a "Visit appflowy.io for docs" line with a real link) — irrelevant, the page is slated for deletion.
- **Visual-line selection shortcuts weren't repeatable — FIXED on first-use feedback, live-verified (2026-07-20 session 4 r2).** Commit `74c0f1ed2`. After Option+Ctrl+Shift+Left/Right reaches a line's edge, pressing the same arrow now takes in the NEXT visual line and the opposite arrow gives lines back one at a time — crossing into the neighboring text block at block boundaries. Verified live in RTL (3 presses walk lines 2/3/4, opposite presses retrace, flows into the next paragraph).
- **RTL selection jumps left when clicking past the text edge — FIXED and live-verified (2026-07-20 session 4).** Fork commit `e7a3942e`, app pin `2ff4ddbe2`. Reproduced live BEFORE coding, exactly as the spec hypothesized: at a soft line-wrap the same integer offset means both "end of the previous visual line" and "start of the next"; `Position` carries no `TextAffinity`, and the caret renderer resolves everything upstream — so gutter clicks beside wrapped RTL lines 2+ threw the caret to the far-left end of the line above (line 1 immune, as the user noticed). Fix: the hit-test's affinity rides along as a display-only hint on a private `Position` subclass, honored **only when upstream/downstream resolve to different lines** — bidi run seams (same line, different x) keep the validated upstream behavior, so the deferred embedded-date caret question is untouched. Failing-then-passing fork regression test + all six gutter/text click cases re-verified live post-fix.
- **Page title reverted to "Untitled" after pasting — FIXED, user-confirmed (2026-07-19).** Commit `6b6fa80b2`. The in-document title seeds its text field **once**, from a `ViewPB` captured when the page was *opened* — still empty-named for a page titled in the same session. Invisible until something disposes the widget, and the editor renders the header as **item 0 of a virtualized list**, so a paste that grows the document destroys and rebuilds it, re-seeding from the same stale empty name. (The sidebar was never affected: it reads a long-lived bloc.) Fix: when seeding finds an empty name, **ask the backend for the current one** — survives any number of rebuilds; scoped to the empty case and re-checks focus after the await so it can't clobber active typing. Also fixed a genuine **upstream** bug in the same file: the view-update listener's parameter shadowed the state field of the same name, so `view = view` was a self-assignment and the field never updated. **Upstream-worthy — small and self-contained; PR not yet offered.**
  - **Process note worth keeping:** the first fix attempt targeted one layer too high (it patched a widget that the paste itself destroys, so `initState` re-read the stale value a microsecond later). It shipped, looked right, and did nothing. Reading the actual disposal path is what settled it.
- **Settings dropdown clipped mid-box — FIXED, verified by driving the real app (2026-07-19).** Commit `6b6fa80b2`. `_RenderDropdownMenuBody.performLayout` sizes *itself* to the incoming constraints but lays the visible text field out against the **menu entries'** intrinsic width — and the entries declare `Size(double.infinity, 29)`. The field is laid out wider than its slot and, since `paint()` draws it at offset zero, it spills past the container edge and is clipped. Fix: plumb `AFDropdownMenu`'s existing `width` param through `SettingsDropdown` so the inner constraint is finite. **A `SizedBox` around the dropdown does NOT fix this** — it bounds the slot, not the field inside it.
  - **Settings dialog width** was also `window.width * 0.6` with no cap; now a fixed **900pt** in one named constant (`_kSettingsDialogWidth`), shrinking only when the window can't hold it. Widened, not narrowed — the complaint was cramped, not oversized.
  - **Process note:** two failed fixes preceded this, both reasoned from screenshots. The third attempt — opening the app with computer-use and zooming into the control — found the true cause in minutes. **For anything visual, look at it before theorising.**
- **macOS asked for Documents-folder access on EVERY launch — FIXED, verified on a fresh binary (2026-07-19).** Commit `6b6fa80b2`. Two causes stacked:
  1. **What:** `FlowyPluginService` — the custom-**theme** loader — resolved its location to `getApplicationDocumentsDirectory()`, which on a **non-sandboxed** macOS build (ours: `com.apple.security.app-sandbox` = `false`) is the real `~/Documents`, then `listSync()`'d it at startup. The app was asking for personal documents to find theme plugins the user doesn't have. Fixed by pointing it at Application Support — the prompt is **gone**, not merely granted.
  2. **Why every time:** the build is **ad-hoc signed** (`Signature=adhoc`, `TeamIdentifier=not set`). macOS keys a **TCC grant to the app's code hash**, and every rebuild changes it — so each build looks like a different app. This is *why it started a few sessions ago*: we only began rebuilding the dock app every session recently. **This generalises: every macOS permission this app is granted dies on the next rebuild.** Recorded in `specs/distribution.md` (open question 2) — it will matter concretely for the microphone-dependent transcription feature.
- **Empty-line cursor ("creating a new line has a very far cursor") — FIXED, confirmed live by the user.** Root cause was the user's setting **`kDocumentAppearanceDefaultTextDirection = rtl`** (document default direction RTL). That made every empty line RTL while showing the LTR English "Type '/'…" placeholder, and two stacked bugs stranded the caret/text far left:
  1. The empty-line caret resolved to logical offset 0 of the LTR placeholder run (its *left* end). The old "correction" was a permanent no-op (it read `_renderParagraph.size.width`, a real 0.0 for empty text, before the placeholder width in a `??` chain). Fixed by SETTING the caret dx to the placeholder paragraph's own (right-aligned) width — the RTL start.
  2. Typing then "jumped left" because the invisible placeholder was kept full-width, inflating the right-aligned RTL block so real text rendered a placeholder-width left of the content edge. Fixed by collapsing the placeholder to an empty span (keeps line height, takes no width) when the line has text.
  Both fixes are in the editor fork's `appflowy_rich_text.dart`. Committed: fork `ba6c4fcb`, app `6ff1967c4`, pin resynced (no drift). Real-target regression test: `integration_test/desktop/document/document_rtl_empty_caret_test.dart`.
- **Floating selection toolbar pointer-anchor experiment — DONE, 2026-07-16, user confirmed happy with "small minor quirks" (unspecified — ask before touching this again).** Four live-tested rounds in one session, each fixing what live testing found wrong with the last:
  - **Round 1:** switched the toolbar's anchor from the selection's raw extent (broke Cmd+A on long pages — landed below the viewport) to a decision tree: mouse pointer if over the visible selection, else ~a third of the way down the visible selection, else the extent clamped into the viewport.
  - **Round 2 (live bugs found: Cmd+A still never showed; felt flickery; RTL not direction-aware):** root-caused Cmd+A to a genuine pre-existing bug in the **editor fork's own** `FloatingToolbar._showToolbar()` — its visibility check collapsed to a sentinel value that suppressed the toolbar even when the selection was genuinely visible; this runs before any app code, so no app-side fix could reach it. Fixed at the fork level (user explicitly authorized crossing into fork core code after Claude Code's own permission system blocked a first silent attempt). Bumped the show-delay 200ms → 400ms. Added LTR/RTL mirroring reusing the existing `documentPopoverDirection` convention.
  - **Round 3 (live bugs found: "pointer over selection" too narrow; RTL fallback anchor pinned to the far-left wall):** widened the pointer check to "anywhere in the visible editor," not just literally over highlighted text. Found and fixed a real RTL bug: the fallback anchor was a 1px-wide point at a selection row's `left` edge, and mirroring RTL by subtracting the toolbar's width from that drove it deeply negative — clamped to the far-left wall regardless of where the text was. Fixed by giving the fallback anchor a real `left..right` span and mirroring RTL off `right` instead. Delay tuned 400ms → 300ms.
  - **Round 4 (refinement, not a bug fix):** the pointer anchor shouldn't sit flush against either side of the pointer — now opens 1/3 of its width *against* the reading direction and 2/3 *with* it (mirrored between LTR/RTL). Applies only to the pointer anchor, not the fallback.
  - **State:** fork commits `80136613` (round 2) + `5354a98d` (round 3 delay tweak) on `rtl-direction-aware-selection-menu`, both pushed to `origin`, pin resynced, no drift. App commits `e9616c11f`, `8943e5466`, `f30231389`. Kill-switch `anchorToolbarToPointer = false` in `toolbar_pointer_tracker.dart` restores pre-experiment behavior exactly (app-side only, no fork revert needed for that). 11/11 app-side widget tests pass, fork's 4-test suite passes, `flutter analyze` clean. **PR #1222 needed no update across any round** — confirmed by diff each time: it's an unrelated timing fix in the same file, zero positioning/gate logic.
  - **Honesty note kept from round 2:** the Cmd+A fork-level fix was never proven by a failing-then-passing test (multiple widget-test constructions failed to reproduce it) — it shipped on code-reasoning confidence. The round-3 RTL far-left-pin fix, by contrast, **is** proven this way. If Cmd+A ever regresses, that's the first place to look.
- **Mid-character cursor in embedded dates — DEFERRED to next session** (user's choice). Deterministic repro is already encoded in the fork's `caret_bidi_test.dart` skipped test. Blocker is an *oracle* — the user needs to look at the one anomalous boundary (right after the comma in an embedded date like `20.4.26,`) and say what "correct" is. Plan next time: capture a real-render screenshot of that spot, get the user's judgment, then fix to match.

## Where things stand
- Repo forked (`origin` = matanrotman/AppFlowy), `upstream` = AppFlowy-IO/AppFlowy.
- **Fork-sync (checked 2026-07-20 session 4, end of session):** app `main` **0 behind** of `upstream/main` (ahead count grows every session — don't trust a written number, re-run the check). Editor fork `rtl-direction-aware-selection-menu` **38 behind / 14 ahead** — upstream moved 2 since last check; still the deliberately-deferred number, see the merge section above before acting on it. **Pin ↔ pushed HEAD: in sync** (`806f9b3a`, verified both sides after this session's two fork commits `e7a3942e` + `806f9b3a`, both pushed).
- **The dock app was rebuilt (twice — after the r2 shortcut/arrow-up fixes, and again after the theme split) and content-verified each time** (test-binding refs 0, `runAppFlowy` > 0, new markers present: `_adjacentVisualLineEdge`, `PageThemeScope`, `chromeAppearance` in bundled translations). It is running via a normal LaunchServices open; quit/reopen is not needed. **Note:** the chrome-theme pref was set and then deleted during live verification, so the app is back on the default ("Same as pages").
- *(Historical, 2026-07-16):* app `main` **0 behind** `upstream/main` — that's the number that matters. (The "ahead" count climbs with every commit here, so don't treat any figure written down as current; the session-start fork-sync check is authoritative.) Editor fork branch `rtl-direction-aware-selection-menu` **36 behind, 12 ahead** of `AppFlowy-IO/appflowy-editor` (tagged 6.1.0; our pin reports 5.2.0) — see the "deferred on purpose" section at the top before acting on that number. Pin ↔ pushed-HEAD: **in sync** (`5354a98d`).
- **Toolbar work is fully committed** (`e9616c11f`, `8943e5466`, `f30231389` on the app side; `80136613`, `5354a98d` on the fork, pushed).
- **✅ Stage 4 backup work is committed as of 2026-07-16 r4 end** (`08066e099`, 9 files: `RESTORE.md`, `restore_service.dart`, `restore_flow.dart`, `settings_backup_view.dart` (Restore… wired), both new restore test files, translations, spec, STATUS.md). Zero core-file touches — `locale_keys.g.dart` stays gitignored/generated, same as Stage 3. Working tree is clean.
- **The dock app was rebuilt during the session with the Stage-4 work in place** and content-verified (test-binding refs 0, `runAppFlowy` > 0, restore strings present) — no `.dart` changes happened after that build, so it's still current; the user should quit + re-open AppFlowy to pick it up if they haven't already.
- **Editor fork upstream merge — deferred indefinitely, on evidence.** Not "risky to fold into a bug-fix session" (the old reason) — it's the wrong move until AppFlowy-IO moves first. See the top section.
- **⚠️ The RTL caret regression test is intermittently unreliable — 1 hang in 3 runs (2026-07-15 r2).** When it runs it is *deterministic*: two passing runs produced bit-identical geometry (`emptyCaret=1174.0 typedCaret=1164.7 diff=9.3`, threshold 999.0 of a 1332px editor). But one run in between **hung for 61 minutes** and failed having never reached the measurement. Root cause **unknown** — the run's output was destroyed by grepping it, so there's no log (see the process rule below). Ruled out: it is not the `setUpAll` guard (present in the passing runs), not the removed `foundation.dart` import (the *later* passing run has it removed), and not a geometry regression (no `RTLDIAG` line = it never measured).
  **How to read a failure:** if there's no `RTLDIAG` line, the test didn't measure anything — treat it as a flake, re-run, and don't conclude the caret regressed. A real regression shows `RTLDIAG` with bad numbers. **Never pipe a test run through `grep`** — capture full output to a file (`… -d macos > run.log 2>&1`) and grep the *file*, or the next failure is undiagnosable too. Worth a proper diagnosis session before leaning on this test as a safety net.
  **Lead for that diagnosis — deliberately left in place:** `~/Library/Caches/appflowy_integration_test` has **52** accumulated run dirs (14M). It's the prime suspect for the hang, so it was *not* cleaned at session end: wiping it would destroy the repro and probably mask the flake. It is test cache only — the real data is `~/Library/Application Support/com.appflowy.appflowy.flutter/data_dev_beta.appflowy.cloud`, a different tree entirely — so it's safe to clear once the diagnosis is done (or if the flake ever blocks real work).
- **✅ Upstream toolbar PR SENT — https://github.com/AppFlowy-IO/appflowy-editor/pull/1222** (opened 2026-07-15 r2 at the user's explicit instruction). Branch `fix/floating-toolbar-debounce-race`, commit `7299f9d4`, 1 file, +24/−9. The debounce-key split + post-frame deferral, rebased onto upstream's newer `dispose()`. If accepted, `floating_toolbar.dart` leaves the fork for good — one less file to reconcile at every future merge.
  - **CLA signed (confirmed 2026-07-16); `license/cla` check passes.** Now waiting on upstream: no other CI has run (first-time contributors usually need a maintainer to approve workflow runs).
  - Never compiled locally (upstream needs Flutter ≥3.32, we're on 3.27.4); verified by inspection only — parses, `dart format`-clean, no dangling `_debounceKey`. **Upstream's CI (Flutter 3.38.5) is the first real compile — check it.**
  - The PR states plainly that no widget test reproduces the race (a `jumpTo()` + settle can't recreate a real drag's continuous scroll-tick stream), so expect maintainers may ask for one.
- **The user's dock app now runs the fixed code with their real data — confirmed live by the user** ("everything's back", cursor fix works). Done by rebuilding the debug app in place (`flutter build macos --debug`), which is what the dock icon points at.
- **Data backup taken 2026-07-15** at `~/Desktop/AppFlowy_data_backup_backup_20260715_0925` (6.2M — all four data folders). **The cloud-sync question is now settled in the worst direction (see top): the cloud holds only empty shells, so this backup + the live folder are the ONLY fully-proven copies of the user's writing. Keep it until Stage 4's restore drill passes** (Google Drive snapshots are already flowing as of 2026-07-16, but nothing has been restored from one yet).
- **Incident history (still governs live testing):** a 2026-07-13 live-drag test corrupted a real user document. Rule since: all live typing/selecting/dragging happens in a disposable scratch page, never in existing content.
  - **Extension added 2026-07-20: a click for *focus* counts too.** Clicking the empty space **below** a document's content appends an empty paragraph — a real, persisted edit. This happened to "משימות 20.7" while positioning for a screenshot; it was caught and undone with Cmd+Z (safe only because the app had just restarted, so the undo stack held nothing else). **To focus the window, click neutral chrome** — the sidebar, the ribbon, the title bar — never the page body.
- **Computer-use `scroll` events do not reach every Flutter scroll view (2026-07-20).** They silently did nothing on both the Settings dialog and the editor's scroll view, while both scroll fine by hand. **A scroll that yields a pixel-identical screenshot is evidence about the tooling, not the app** — it already produced one false bug report ("Settings → Workspace doesn't scroll"), since retracted. Verify scroll-dependent behaviour with the user instead of concluding from the tool.
- **⚠️ Computer-use CLICKS can drift off-target after a mid-session display/window change (2026-07-20 session 4).** After the app window came back at a different size, every `left_click` landed exactly **94px ABOVE** the intended point, while `mouse_move`, hover reactions and `cursor_position` all stayed accurate — so "hover works but clicks do nothing" is a tooling symptom, NOT an app bug. One mis-mapped click hit "New page" and created a junk page in the real workspace. **Calibrate before precision clicking**: issue a click at a harmless target, read `cursor_position`, and add the measured offset to every subsequent click. Two follow-on facts: (1) keyboard `key` events are unaffected; (2) even with compensation, **popover triggers (sidebar `…` menus, right-click context menus) never opened from synthetic clicks** in this state — row navigation, buttons, caret placement and text selection all worked. If popovers won't open, stop and leave that interaction to the user rather than experimenting inside real data.
- Local build confirmed working: `flutter run -d macos` and `flutter build macos --debug` (the dock app). A release build also compiles, but it is **not** the user's app — see rule 2 above.
- Toolchain (unchanged): Rust rustup w/ pinned 1.85 + stable default; `cargo-make` + `duckscript_cli` (`duck`); **Flutter 3.27.4** git-cloned at `~/flutter` (not Homebrew); CocoaPods/sqlite3/protobuf via Homebrew.
- Rust core (dev): `cargo make --profile development-mac-arm64 appflowy-core-dev-macos` (from `frontend/`). Release core: `cargo make --profile production-mac-arm64 appflowy-core-release`.
- Code generation before `flutter run`: `cargo make code_generation` (from `frontend/`).

## Next step
0. **User homework from 2026-07-20 session 4: delete three junk test pages** sitting at the top of the General space — "zz scratch - selection test" (the RTL test page) and **two empty "Untitled" pages** (created accidentally when computer-use clicks landed 94px off-target — see the hazard below). Right-click → Delete on each; they contain nothing of value. Claude deliberately did NOT delete them: the click pipeline was misbehaving and GUI deletion inside the real workspace wasn't worth the risk.
*(The 2026-07-20 "START HERE" items are both DONE and live-verified — RTL selection-jump bug fixed, Option+Ctrl+Shift+arrow shortcuts built, collapse shortcut default picked (Cmd+Option+R). Detail: `specs/rtl-support.md` Session Log, session 4.)*
1. **Verify the remaining Phase 2 items live**, then commit. Still unverified at the time of writing: the RTL-page title/margin case (LTR page was verified), and a measured before/after of the rendered text edges.
2. ~~Settings → Workspace does not scroll.~~ **NOT A BUG — retracted 2026-07-20.** Claude reported this after synthetic scroll events failed to move the dialog; the user confirmed it scrolls fine by hand. The lesson is about the tooling, not the app: **a computer-use `scroll` that does nothing is not evidence of a broken scroll view.** The floating-toolbar toggle is reachable and therefore still unverified rather than blocked.
3. **Database row documents are not direction-aware.** (Plain language: opening a *row* of a database gives you a small document page of its own; that page runs on a separate code path from a normal page and still always assumes left-to-right, so an RTL row-document's margins sit on the wrong side.) `database_document_page.dart` uses the LTR-shaped `EditorStyleCustomizer.documentPadding`. The header-alignment half was fixed 2026-07-20; the direction half was deliberately left — it needs the same page/global/layout resolution the main document page does, and there was no database row to verify against. Low priority: only bites if the user writes RTL inside database rows.
4. **Keyboard shortcuts as a whole remain deferred** (user, 2026-07-20) — but the one urgent piece is now done: **the ribbon collapse toggle defaults to Cmd+Option+R on macOS** (commit `b66ccdcc9`, verified live both ways; Ctrl+F1 kept for Windows/Linux). The new selection shortcuts (Option+Ctrl+Shift+arrows) also shipped this session and appear in Settings → Shortcuts automatically. The rest (H4–H6 bindings, rebind bugs, typed-character shortcuts) stays deferred.
*(Historical, now done: get sign-off on `specs/ribbon-menu.md`, then build Phase 1.)* The interview is done and the spec is written; read it first. Phase 1 = the tabbed frame (four tabs), collapse via chevron + rebindable shortcut with globally-persisted state, RTL mirroring, the `ribbonMenu` feature flag, the floating-toolbar suppression + its new settings toggle, every ✅ button wired to its existing command, and every ⚠️/❌ item present as a **visibly disabled** "coming soon" button. Build the button component with **tooltip = name + shortcut** from the start — retrofitting it later would touch every button. **Handle the `editorState.selection!` force-unwrap first** (see "Active feature") — it's the most likely Phase 1 bug. Verify on the real macOS target, not headless.
1. **Google Drive backup — DONE for practical purposes; drill (f) passed 2026-07-19.** The 2026-07-15 Desktop backup folder is retirable. Optional leftovers only: (a-web) confirm uploads in drive.google.com; (c) evict-and-restore an offloaded snapshot; (d)/(e) quit and `kill -9` paths; (g) the seeded-prune drill (needs the user's go-ahead — touches the real Drive). One small known follow-up: cap `collab_db_history` at the newest ~3 *inside* snapshots, since AppFlowy adds one archive per day forever and they're already the largest part of each zip.
2. **Floating toolbar — if the user brings up "small minor quirks" again, ask what they are first.** The pointer-anchor experiment is done and the user is happy overall (see "Bug status"); they mentioned unspecified small quirks in passing but didn't ask for further work. Don't guess at what they meant — ask, then scope from there like any other feature request.
3. **Shortcuts: user homework + two known bugs.** The user should try Settings → Shortcuts (search/rebind/conflict detection already ship; persisted to `<data>/shortcuts/shortcuts.json`) and report real gaps. Known gaps: no H4–H6 bindings; typed character shortcuts (`# ` → heading) aren't rebindable; persistence stores one binding per key with no platform field. Two real bugs found 2026-07-16, fix when this becomes active: (a) a cleared binding silently returns after restart (`clearCommand()` persists `command: ''` but `updateCommand('')` no-ops on load — editor fork `command_shortcut_event.dart:99`); (b) commands with `getDescription: null` render as blank rows in the settings list. Good news: new shortcuts are app-side only (`editor_plugins/shortcuts/command_shortcuts.dart:19`) and appear in the settings UI automatically — zero fork changes.
4. **Mid-character cursor bug** — capture a real-render screenshot of the caret right after the comma in an embedded date, get the user's judgment on correct placement, fix to match, un-skip the `caret_bidi_test.dart` case.
5. **Toolbar PR #1222 — CLA now SIGNED (2026-07-15), but upstream CI has not run** ([PR](https://github.com/AppFlowy-IO/appflowy-editor/pull/1222) shows only the CLA check; a maintainer likely must approve first-contributor workflows). When CI runs, it's the first real compile this fix has ever had — check it. Maintainers may ask for a reproducing test the PR already admits doesn't exist. (Unaffected by this session's further fork commits — confirmed each round the PR touches nothing this session changed.)
6. **Automate the three footguns** — a single wrapper script for real-target tests: run the test → `flutter build macos --debug` (restore the dock app from the test build) → `defaults delete … path_location` → verify by contents (`grep -c IntegrationTestWidgetsFlutterBinding` = 0). Doing these by hand has failed before.
7. **Bidi arrow-key navigation** (`specs/rtl-support.md`, scoped 2026-07-16) — visual caret movement; own-session-sized; editor-fork change, possibly upstream-worthy.
8. **Editor-fork upstream merge — do NOT schedule.** Deferred indefinitely; see the merge section for the trigger. Listed here only so its absence isn't read as an oversight.

## Open questions
- Sharing-scope badge / workspace icon in the content-pane toolbar — user deferred (out of scope for now).

## Written specs not currently scheduled
*(listed so they don't get lost between sessions)*
- `specs/distribution.md` — **new 2026-07-18, placeholder, needs its own scoping interview.** Goal: others download a prebuilt **macOS** build instead of compiling. Leaning decision recorded: keep the AppFlowy name + honest qualifier ("RTL build" / "community build"), change only the bundle id + data-folder location so it coexists with official AppFlowy — **no rebrand, not a separate product.** Open: exact qualifier/bundle id, **one-time migration of the user's existing data when the bundle id changes** (take a backup first — blank-window hazard), code-signing/notarization (~$99/yr Apple account vs. Gatekeeper warnings), what server a fresh download defaults to (cloud sync is dead → likely local-only), AGPL obligations, GitHub Releases hosting, manual-update expectation.
- `specs/tables.md` (RTL table paste) — written spec, untouched since 2026-07-14, not scheduled.
- `specs/meeting-transcription.md` — stub only (2026-07-12): connect the app to the user's **self-hosted** transcription server. "Not started, no integration exists yet." Still needs the scoping interview before any code — its six open questions (streaming vs batch API, live vs post-hoc, where transcripts land, speaker labels, UI trigger, offline/unreachable handling) are unanswered. Note the CLAUDE.md privacy rule applies here and this is the exact case it names: flag the external-service trade-off explicitly and use the user's own config — though self-hosted is the mitigation, so confirm what the server actually is before assuming.
- `specs/plugin-system.md` — **RESOLVED 2026-07-17, no longer a question**: no plugin system gets built; sidecar modules are the standing pattern. Now serves as (a) the decision record so this is never re-litigated, (b) the architecture-facts dossier for the ribbon build, and (c) the prepped ribbon scoping interview. The ribbon itself is the active feature (see top). Side note kept: per-page RTL/LTR (via `View.extra`) remains independently feasible and small, no ribbon needed.

## Local build quick-reference
Run from `~/Projects/AppFlowy/frontend`, with `~/flutter/bin`, `~/.cargo/bin`, `~/.pub-cache/bin` on PATH:
```
# Dev run (debug; uses the user's real data_dev* workspace):
cargo make --profile development-mac-arm64 appflowy-core-dev-macos
cargo make code_generation
cd appflowy_flutter && flutter run -d macos

# ⭐ SHIP A FIX TO THE USER'S DOCK APP (this is the one that matters):
#    rebuilds in place at exactly the path the dock icon points to.
cd appflowy_flutter && flutter build macos --debug
#    verify it took by CONTENTS — NOT by timestamp. Flutter preserves the cached
#    artifact's mtime, so a correct rebuild can legitimately show an OLD time.
KB=build/macos/Build/Products/Debug/AppFlowy.app/Contents/Frameworks/App.framework/Resources/flutter_assets/kernel_blob.bin
strings "$KB" | grep -c IntegrationTestWidgetsFlutterBinding   # want 0 (non-zero = TEST build)
strings "$KB" | grep -c runAppFlowy                            # want >0 (the real app)

# Real-target regression test (the ONLY trustworthy way to check RTL geometry):
flutter test integration_test/desktop/document/document_rtl_empty_caret_test.dart -d macos

# ⚠️ ALWAYS run BOTH of these afterwards — the test run leaves the dock app broken in
#    two independent ways, and each looks identical to "the build is broken":
flutter build macos --debug   # 1. the test run replaced the dock app with a TEST build
defaults delete com.appflowy.appflowy.flutter flutter.io.appflowy.appflowy_flutter.path_location
                              # 2. it also pointed the app at an empty test sandbox
# Then verify by CONTENTS (not timestamp — see rule 2 above for why):
KB=build/macos/Build/Products/Debug/AppFlowy.app/Contents/Frameworks/App.framework/Resources/flutter_assets/kernel_blob.bin
strings "$KB" | grep -c IntegrationTestWidgetsFlutterBinding   # want 0
strings "$KB" | grep -c runAppFlowy                            # want >0

# Release build: NOT what the user runs. It targets the data* workspace, which is
# a different (stale) cache — building it made pages look "missing". Avoid unless
# genuinely packaging a release.
```
